import {useRef,useState} from "react";
import type {CredentialProbe} from "./credential-store.js";
const messages:Record<CredentialProbe,string>={present:"Clave guardada en el llavero.",missing:"No hay una clave guardada.",saved:"Clave guardada. Volvé a abrir el proyecto para usarla.",deleted:"Clave eliminada. Volvé a abrir el proyecto.",cancelled:"Configuración cancelada; se conserva la clave anterior.",locked:"Desbloqueá el llavero en Acceso a Llaveros y reintentá.",unavailable:"No se confirmó el acceso al llavero. Consultá el estado antes de reintentar.",invalid:"La clave no es válida: usá un valor sin espacios ni saltos de línea.",unsupported:"El almacén seguro solo está disponible en macOS.",connected:"Conexión autorizada para consultar modelos; no se ejecutó una generación.","invalid-credential":"El proveedor rechazó la clave. Configurá una nueva.",forbidden:"La clave no tiene permiso para consultar modelos.","rate-limited":"El proveedor limitó la consulta. Reintentá más tarde.","network-error":"No se pudo verificar la conexión. Revisá la red y reintentá."};
export function CredentialSettings(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");const lock=useRef(false);
 const act=async(operation:()=>Promise<CredentialProbe>)=>{if(lock.current)return;lock.current=true;setBusy(true);try{setMessage(messages[await operation()]);}catch{setMessage("Hay trabajo activo o no se confirmó su cierre. Esperá a que termine antes de cambiar credenciales; si el error persiste, reiniciá la aplicación.");}finally{lock.current=false;setBusy(false);}};
 return <details className="credential-settings"><summary>Credenciales de OpenAI</summary><p>La clave se introduce en una ventana segura de macOS y se guarda en el llavero. Cerrá las tareas activas antes de reemplazarla o eliminarla.</p><button disabled={busy} data-action="credential-status" onClick={()=>void act(()=>window.harness.credentialStatus())}>Consultar presencia</button><button disabled={busy} data-action="credential-configure" onClick={()=>void act(()=>window.harness.configureCredential())}>Guardar o reemplazar clave</button><button disabled={busy} data-action="credential-probe" onClick={()=>void act(()=>window.harness.probeCredential())}>Probar conexión</button><button disabled={busy} data-action="credential-delete" onClick={()=>void act(()=>window.harness.deleteCredential())}>Eliminar clave del llavero</button>{message?<p role="status">{message}</p>:null}</details>;
}
