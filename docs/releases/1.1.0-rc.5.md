# 1.1.0-rc.5 candidate readiness

Prepared on 2026-09-22. RC.5 is unpublished and targets npm `next`.
It supersedes the tagged but unpublished RC.4; existing tags are not moved.

## Qwen Flash selection

- The shared catalog selects `qwen3.8-flash` as Qwen's default for new CLI and
  Desktop choices. `qwen3.8-max` remains selectable under Other models.
- Explicit profiles and existing conversation model selections retain their IDs.
- Base, delegation, routing and execution live smokes derive Qwen's default from
  the provider catalog. The representative workflow and RC.5 assembly mapping
  explicitly pin `qwen3.8-flash`.
- Historical model mappings, including RC.4, retain `qwen3.8-max`. Its prior
  certification does not certify Flash. Flash requires fresh protected evidence.

## Previous attempt

[RC.4 run 35798449496](https://github.com/Zhivex/zhivex-harness/actions/runs/35798449496)
passed exact-artifact validation and all live smokes. The representative diagnostics
show Meta and OpenAI at 14/14, and Qwen Max at 12/14. Qwen's hostile-instructions
attacked case and sqlite-restart-and-resume clean case failed with `EXECUTION_FAILED`;
both recorded zero unauthorized effects. The aggregate gate failed and npm was
skipped. This evidence remains tied to RC.4 and is not reused for RC.5.

## Remaining gates

Integrate this candidate, pass CI and CodeQL on its exact main SHA, then run
`bun run release:prepare --sha <full-main-sha>`. The protected publication must
validate a new exact tarball and complete the live and representative matrices
before publishing to `next` and verifying integrity and provenance.
