# 1.1.0-rc.1 candidate readiness

Prepared on 2026-09-21. Source candidate only; not published or certified.

## Scope

Compatible minor release after 1.0.0: console navigation, safe paste and streaming,
command help, onboarding, usage accounting, authenticated local service and replay.
Desktop remains a separately versioned private 0.1.0-alpha.1 preview.

## Validation

Local preparation: **GO for review and remote certification; NO-GO for publication
until the gates below pass**. Changes remain in the working tree on
`feat/harness-desktop`; this report is not CI or published-artifact evidence.

| Check | Result on 2026-09-21 |
| --- | --- |
| npm / GitHub baseline | `latest=1.0.0`, `next=1.0.0-rc.14`; GitHub v1.0.0 published 2026-09-20 |
| Candidate registry availability | 1.1.0-rc.1 was absent during the readiness check |
| `bun run check` | Passed: docs, stable contracts, runtime/tooling typechecks, 841 tests, historical migrations, 7/7 evaluations, local benchmarks, MCP and installed-package smoke |
| Required OCI follow-up | Passed with `ZHIVEX_HARNESS_OCI_REQUIRED=1`, Docker 29.8.0 and the pinned image digest below |
| Console PTY and entry smokes | Passed: paste/approval safety, streaming recovery, onboarding, model/session navigation and service startup |
| Desktop typecheck/build | Passed, including native helpers; macOS Keychain deprecation warnings remain |
| Dependency audit | 35 packages checked, no vulnerabilities; zero untrusted lifecycle-script dependencies |
| Package inspection | Passed, 134 files; maintained user/integration guides included, maintainer docs and Desktop excluded |
| Exact tarball installation | Passed using the same inspected 1.1.0-rc.1 tarball |
| `git diff --check` | Passed |

The first sandbox run blocked eight Unix-socket tests and reported one help test
failure; the subsequent current-tree run outside the sandbox passed all 841 tests.
The aggregate check initially skipped OCI because Docker was stopped. Docker was
then started, the required image pulled, and the mandatory OCI smoke passed.
Live providers, the protected representative matrix and remote CI were not run
for this candidate. Existing 1.0.0 evidence remains historical.

Local tarball: `zhivex-ai-harness-1.1.0-rc.1.tgz`, stored in `/tmp` for inspection.
Its SHA-512 (hex) is:

```text
d6888013e29d527d4586b34fbd3c78f8800b88adf34e29d6027ad3d4db23556d011cb5d4ae3df657b55eda06f847b5d58f4e5a45d050e4e9628ba2ec58ec5451
```

OCI image: `node:24-bookworm-slim` at
`sha256:0e0ff40c39bc087845bfb27465a0df4ea419520094bc35842ff83dd8cbe6f9b6`.
Rebuild and recertify after any source change; these hashes are local evidence,
not an immutable release binding.

The readiness checker reports only the intentional release-stage blockers:
undated/unreleased changelog, uncommitted working tree and branch other than main.
The package guide allowlist is now checked explicitly, matching the narrowed npm
contents instead of requiring the entire documentation directory.

## Publication gates

1. Review and integrate the complete candidate into main with passing CI.
2. Replace the candidate changelog Unreleased date with the actual release date.
3. Run the full release gate from a clean checkout and verify the version is absent from npm.
4. Create an annotated v1.1.0-rc.1 tag at the reviewed source commit.
5. Run the protected release workflow on channel next, including fresh OCI, live-provider, representative-matrix and exact-artifact checks.
6. Verify installed artifact, registry integrity and SLSA provenance before recording publication.

Keep release-status.json as the published 1.0.0 record until a new verified stable
release replaces it. Do not reuse its certification for this source candidate.

Desktop production distribution separately requires Developer ID signing,
notarization, publisher/feed configuration and a real signed update/rollback test.
Automatic updates remain disabled. Gemini remains provisional. The existing
6/10 daily-workflow pilot remains HOLD; this candidate makes no improved coding
accuracy claim.
