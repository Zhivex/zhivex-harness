# 1.1.0-rc.9 preparation

This candidate is unpublished and has no tag. It follows immutable RC8; never
move or reuse its tag.

- New Meta selections use `muse-spark-1.3`; saved selections retain their model IDs.
- Built-in Meta tool loops use Responses for generation and streaming. Durable
  Meta bindings change so old Chat runs cannot silently resume under Responses.
- Representative release pins: Meta `muse-spark-1.3`, Qwen `qwen3.8-flash`, OpenAI
  `gpt-6-luna`. Historical mappings are unchanged.
- Integrates governed delegation contracts and the SDK next batch from PR #102.
- Orchestration diagnostics identify the failing checkpoint and preserve sanitized
  terminal provider/SDK errors instead of replacing them with a generic assertion.

Publication remains blocked pending live gates and the exact-artifact representative
matrix. Local checks, diagnostic passes and a prepared version do not authorize
skipping gates. The development readiness report retains failed campaigns.

Release preparation also runs `check:sdk-redaction` before expensive gates. This
offline synthetic check currently blocks on the installed SDK redaction-state
defect; see the linked investigation report.
