# HAR-HU-32 — Reviewed PR creation and local acceptance

Status: implemented and verified locally; pending publication. Branch:
`feat/harness-desktop`. No implementation-branch push or real GitHub PR was made.

The desktop now reviews the exact title/body, draft choice, selected repository,
published head/base refs and SHAs, and complete commit/file history before a
separate creation authorization. Main validates sender/project/payload and applies
the existing runtime mutation admission and shutdown drain. Credentials remain in
host gh; renderer storage contains only operation IDs. PR links are opened by main
only after reconciling a known operation and validating its repository/number/URL.

Lost responses remain pending and are reconciled without another POST. The last
confirmed PR can be restored after whole-app restart. Changed head/base during
creation retains the observed result and requires review; definitive rejection
permits a fresh proposal, while uncertain outcomes never automatically replay.

## Acceptance evidence

| Criterion | Evidence |
| --- | --- |
| Review staged files/message/branch/destination before commit, push and PR | Existing commit and push UI plus PR review display exact snapshots and separate authorization buttons. Packaged worktree journey exercises all three. |
| Preserve foreign files; filter sensitive content; isolate credentials | Git delivery tests and packaged root/sibling worktree preservation; raw multiline secret, protected path and incomplete preview tests; isolated Git network store and literal gh JSON stdin. Detection is bounded, not proof against all arbitrary secrets. |
| Recover conflicts, stale remote and network failures without duplication or implicit force | Commit CAS/conflict tests, push remote-race tests, PR stale/existing/race/rejection/unknown tests; packaged lost responses at API and renderer boundaries followed by reload/restart, one commit, one push and one PR POST. |

## Validation

- `bun test`: 695 pass, zero failures, 3985 assertions across 95 files.
- Root, tooling and desktop typechecks passed; contracts and documentation passed.
- Development worktree journey passed: `/tmp/har-tasks-2M8FIE/report/report.json`.
- Unsigned macOS arm64 package worktree journey passed:
  `HAR_HU_32_PR_UI_PACKAGED_2026-09-20.json`.
- General packaged regression: `HAR_HU_32_PR_REGRESSION_2026-09-20.json`.
- ASAR size/hash: `HAR_HU_32_PR_CANDIDATE_2026-09-20.json`.

The fake gh stores a local PR before dropping its API response; main then drops
its successful renderer response. Reload reconciliation and whole-app restart
recover the same PR, and the fixture link opener verifies its exact URL twice.
The test verifies one POST and one PR, one ordinary push to a local bare remote,
original branch preservation, sibling isolation and temporary network store cleanup.
The API body contains literal HTML-looking text, rendered as text in the review.

The first packaging attempt failed because the sandbox could not resolve
`github.com` for Electron download. Repeating the authorized local package build
with network permission succeeded. This was not a publication attempt.

## Limits

GitHub API/authentication is simulated; no live credential certification is claimed.
Only configured GitHub HTTPS remotes are supported by this adapter. The candidate
is unsigned/unnotarized, as authorized; its ASAR hash does not certify the entire
Electron bundle. External concurrent formatting edits remain outside this commit.
The six touched tracked source files produce identical Bun-minified output between
the tested working files and the staged semantic increment without that formatting.
HU33–35 remain separate work; this closes only the local implementation of HU32.
