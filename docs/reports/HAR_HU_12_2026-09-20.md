# HAR-HU-12 — Stable 1.0 onboarding

> Historical snapshot: findings and outcomes apply to the checkout and attempt
> recorded below. For the subsequent stable release outcome, see
> [current release evidence](../LIVE_CERTIFICATION.md#current-public-status).
> Later publication does not change failed evaluation results.

Date: 2026-09-20. Scope: documentation and its regression guard. No runtime, configuration schema, persistent state, dependency, or release-version changes.

## Implementation

README installation, daily workflow and provider evidence now identify 1.0.0. The roadmap separates current GA status from historical checkpoints and no longer lists 1.0 as planned. CLI, support and the documentation index link the stable journey. `examples/README.md` provides an isolated Bun installation and explains sample evidence.

`bun run docs:check` now includes example Markdown links, rejects obsolete or floating package pins in first-run guides, rejects stale candidate claims, requires version/help/doctor commands and a published roadmap row. Historical reports, migration instructions and changelogs retain their dated evidence.

## Reproducible verification

Run the commands in [the isolated installation example](../../examples/README.md). For a credential-free reproduction, launch them with a temporary HOME/cache and an environment containing only PATH, HOME, TMPDIR, BUN_INSTALL_CACHE_DIR and NO_COLOR; do not copy a repository `.env` or provider keys.

Observed on macOS with Bun 1.4.0 and Node 22.21.0, in a new temporary consumer with a fresh Bun cache:

| Check | Result |
| --- | --- |
| `bun add --exact --ignore-scripts @zhivex-ai/harness@1.0.0` | Exit 0; published package and nine total packages installed |
| Installed `zhx --version` and `zhivex-harness --version` | Exit 0, `1.0.0` |
| Installed `zhx --help` and `zhivex-harness --help` | Exit 0, versioned command help |
| Both installed aliases: `doctor --provider openai --json` | Exit 3; schema 1, harness 1.0.0, missing OpenAI credential |
| `bunx @zhivex-ai/harness@1.0.0 --version` and `--help` | Exit 0, version 1.0.0 and command help |
| `bunx @zhivex-ai/harness@1.0.0 doctor` | Exit 3; missing selected-provider credential; Git/scripts warnings for the empty consumer |
| `bun test tests/onboarding-docs.test.ts` | 3 passed, 10 assertions |
| `bun run docs:check` | Passed, including examples |
| `bun run typecheck:tooling` | Passed |
| `bun run contract:check` | Passed, no open GA blockers reported |
| `git diff --check` | Passed |

The initial restricted-network download failed DNS resolution; the network-enabled retry above succeeded. No credentials were supplied, no provider calls were made, and no new live certification or release was performed. The global installation variant was not exercised; the isolated installed aliases and pinned bunx path were exercised. These documentation changes require delivery separately from the already-published 1.0.0 artifact.
