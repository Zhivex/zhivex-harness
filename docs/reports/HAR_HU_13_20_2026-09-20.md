# HAR-HU-13–20: implementation and evidence

Date: 2026-09-20. Branch: `feat/har-hu-13-20-daily-cli`.
Live follow-up: authorization was subsequently granted and all 22 attempts completed. HU-14 and HU-20 are complete as measurements, with **HOLD** for release. See `HAR_HU_14_20_LIVE_2026-09-20.md`; the pending-authorization section below records the earlier checkpoint.

Starting checkout: `94bd3aba4ffeaa6b4dd922c0a61ebebda1814cb4` (HU-15 already implemented).
Candidate package version remains 1.0.0; these changes have not been published.

| HU | Local outcome | Evidence |
| --- | --- | --- |
| 13 | Complete | `docs/FIRST_USE.md`, runnable example, installed-package first-use smoke with isolated HOME/profile, edit approval, check, diff and credential/provider/OCI recovery fixtures |
| 14 | Complete after authorization | Frozen 10-task evaluation cohort plus two separate development tasks; independent correctness oracles validated offline for all 10 tasks. Baseline executed: 6/10 correct, all attempts retained in live report |
| 15 | Complete, revalidated | Existing implementation plus extended installed Node PTY smoke |
| 16 | Complete | Literal project-scoped session search before limit, rename/restart, durable state reconciliation and pending payloads before continuation |
| 17 | Complete | Attachment validation and truncation, rules and retained skill receipts, exclusion policy and compaction thresholds; detach affects the next request |
| 18 | Complete | Exact approval identity, add/modify/remove/move scope, pending verification and check failure rendering; existing stale-patch and denial regressions retained |
| 19 | Complete | Durable SQLite per-route ledger, shared child-call accounting, resumed policy, concurrent reservations and fail-closed missing/stale pricing or unknown usage |
| 20 | Complete after authorization; release HOLD | Installed CLI PTY pilot executed on both snapshots; candidate 6/10 correct, comparison and prioritized frictions in live report |

## Validation

- `bun test`: **610 passed, 0 failed**, 75 files, 3,477 assertions.
- Type checking, build, stable API signatures and public contract checks passed.
- Documentation checks and historical migration fixtures passed.
- `bun --no-env-file run smoke:artifact`: passed for the freshly built, packed and installed package. Includes Node PTY and first-use edit/approval/check/diff flow with offline provider fixtures.
- `docs/reports/HAR_HU_14_COHORT_PREPARATION.json`: 10/10 independent oracle preparations passed with no provider requests. This is preparation evidence, not model performance.

## Accounting boundaries

Confirmed provider token usage and estimated USD are separate. Prices require source, effective date and expiry; absent or expired prices leave cost unknown and prevent calls under a monetary cap. Unknown failed/interrupted calls also prevent further capped calls. A resumed run uses its durable original policy; importing a snapshot without its ledger cannot reset its cap. Older runs without ledger history are identified as incomplete history.

Limits apply per logical run, including its child calls and resumptions. A new chat turn starts a new run. Preflight input reservation is a heuristic and estimated cost is not an invoice or a guaranteed billing ceiling. Monetary limits require a supported output cap; unsupported Qwen caps fail closed. Existing legacy token/cost policies remain distinct and conflicting monetary policies are rejected.

## Earlier checkpoint: frozen evaluation and then-outstanding authorization

Dataset: `evaluations/daily-cli-cohort.json`. Four reading tasks, four seeded repairs and two functional changes, with two development tasks kept separate. Evaluation permits one attempt per task/artifact and preserves failures, incomplete tasks, unknown cost and interventions. The PTY approval driver permits only predeclared edit paths and the exact independent pilot check.

Repository snapshots: Harness `94bd3aba4ffeaa6b4dd922c0a61ebebda1814cb4`, TypeScript SDK `65a4a7748c489978b45908a63a05be0e2e63497e`. Runs use disposable copies; original repositories are not pilot edit targets. Baseline is the installed Harness artifact built from `c7d0799`; candidate must be rebuilt from the implementation commit before evaluation.

Automatic approval review rejected the requested live development run because it would send prompts and potentially private repository code to OpenAI and incur credentialed API cost without authorization for that concrete destination/payload. **No live pilot calls were executed.** Explicit user authorization is pending for OpenAI `gpt-5.6-luna`: two development tasks and ten evaluation tasks per artifact (baseline and candidate), each capped at 60,000 input tokens, 4,096 output tokens and 150 seconds. No verified price was supplied, so no monetary estimate is claimed.

Release decision: **HOLD pending actual baseline and candidate pilot evidence**. HU-14 and HU-20 remain open. Offline tests do not establish live provider certification, competitive parity, publication or remote CI success.
